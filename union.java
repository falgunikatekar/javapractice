package hashmap;
import java.util.HashSet;
public class union {
    HashSet <Integer> set=new HashSet<>();
    for(int x:arr1){
        set.add(x);
    }for(int x : arr2){
        set.add(x);
    }return set;
}
