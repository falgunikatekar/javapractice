public class approch2 {
    //boolean array hoga default value will be false and another array flag starting from 0 to 5 it will have false
    //initially and if if the element is there in array and we will mark flag as truee and if the next element is algready 
    //true return that element
    boolean[] flag=new boolean[n+1];
    for(int i=0;i<nli++){
        int element =arr[i]
        if(flag[element]==true)
            break
        else
            flag[element]=true
    }
}
